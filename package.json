{
  "name": "@mimicprotocol/tooling",
  "private": true,
  "version": "0.0.1",
  "description": "Mimic Protocol Tooling",
  "license": "GPL-3.0",
  "scripts": {
    "prepare": "yarn build",
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "workspaces": {
    "packages": [
      "packages/lib-ts",
      "packages/cli",
      "packages/test-ts",
      "packages/integration"
    ]
  }
}
