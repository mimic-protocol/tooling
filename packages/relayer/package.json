{
  "name": "@mimicprotocol/relayer",
  "version": "0.0.1",
  "license": "Unlicensed",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "yarn build && node ./dist/bin/relayer.js --dir ../example/build/",
    "lint": "eslint .",
    "test": "yarn build && yarn start | grep -q 'Task executed successfully' && echo 'test passed'"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "eslint-config-mimic": "^0.0.3",
    "typescript": "^4.0.0"
  }
}