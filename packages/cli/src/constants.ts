export const DEFAULT_MANIFEST_FILE = 'manifest.yaml'
export const DEFAULT_TASK_ENTRY = 'src/task.ts'
export const DEFAULT_TYPES_OUTPUT = './src/types'
export const DEFAULT_BUILD_OUTPUT = './build'

export const DEFAULT_TASK_CONFIG = {
  manifest: DEFAULT_MANIFEST_FILE,
  entry: DEFAULT_TASK_ENTRY,
  types: DEFAULT_TYPES_OUTPUT,
  output: DEFAULT_BUILD_OUTPUT,
}
