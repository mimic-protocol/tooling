{
  "name": "@mimicprotocol/task",
  "version": "0.0.1",
  "license": "Unlicensed",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mimic compile",
    "test": "unset PREFIX && . ~/.nvm/nvm.sh && nvm use 16 && asp",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@as-pect/cli": "^8.1.0",
    "assemblyscript": "^0.27.31",
    "eslint-config-mimic": "^0.0.3",
    "typescript": "^5.5.4"
  }
}
