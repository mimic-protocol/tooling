{
  "name": "@mimicprotocol/example",
  "version": "0.0.1",
  "license": "Unlicensed",
  "private": true,
  "scripts": {
    "postinstall": "yarn build",
    "build": "mimic compile && mimic codegen -o src/types",
    "lint": "echo 'no lint for now'",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@mimicprotocol/cli": "0.0.1",
    "@mimicprotocol/lib-ts": "0.0.1"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.31"
  }
}