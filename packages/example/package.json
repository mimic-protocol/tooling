{
  "name": "@mimicprotocol/sample-task",
  "version": "0.0.1",
  "license": "Unlicensed",
  "private": true,
  "scripts": {
    "postinstall": "yarn build",
    "build": "mimic build --task src/task.ts --output build"
  },
  "dependencies": {
    "@mimicprotocol/cli": "0.0.1",
    "@mimicprotocol/lib-ts": "0.0.1"
  }
}
