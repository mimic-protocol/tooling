{
  "name": "@mimicprotocol/integration",
  "version": "0.0.1",
  "license": "Unlicensed",
  "private": true,
  "scripts": {
    "prepare": "yarn build",
    "build": "rm -rf dist && tsc",
    "test": "yarn build && ts-mocha ./tests --recursive --extension .spec.ts --exit --timeout 5000",
    "lint": "eslint ."
  },
  "dependencies": {
    "@mimic-fi/helpers": "^1.0.0",
    "@mimicprotocol/cli": "0.0.1",
    "@mimicprotocol/lib-ts": "0.0.1",
    "dotenv": "^16.3.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/chai": "^4.3.5",
    "@types/mocha": "^10.0.1",
    "axios-mock-adapter": "^2.0.0",
    "chai": "^4.3.7",
    "eslint-config-mimic": "^0.0.3",
    "mocha": "^10.2.0",
    "nodemon": "^2.0.22",
    "sinon": "^18.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "5.5.4"
  }
}